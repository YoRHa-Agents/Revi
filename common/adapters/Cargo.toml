[package]
name = "common_adapters"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
common_core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
